query CartTransformRunInput {
  cart {
    lines {
      id
      quantity
      customizationId: attribute(key: "_customization_id") { value }
      feeComponent: attribute(key: "_customization_fee_component") { value }
      merchandise {
        __typename
        ... on ProductVariant {
          id
        }
      }
    }
  }
}
